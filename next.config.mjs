/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "m.media-amazon.com",
      "www.cnet.com",
      "https://m.media-amazon.com",
    ],
  },
};

export default nextConfig;
